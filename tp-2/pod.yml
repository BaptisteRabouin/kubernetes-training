apiVersion: v1
kind: Pod
metadata:
  name: my-first-pod
  labels:
    app: simple-webapp
spec:
  containers:
  - name: simple-webapp
    image: mmumshad/simple-webapp-color
    env:
      - name: APP_COLOR
        value: red
